1) Create virtual environments 
    -okx 
    -venv
    Why? There are conflicting websocket class and libraries in different exchanges
2) enter directory 'cd /var/www/html/orderbook'
3) start bash scripts
4) okx folder is for okx library
5) htx folder is for htx library
6) okx2 and htx2 folders are customised codes adapted from okx and htx to accomodate specific requirements
7) redis_channel folder is to test redis client and server
8) strategies folder is to add strategies